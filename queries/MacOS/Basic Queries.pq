# List all machines
    endpoint.name = *
# searching by agentname is unreliable due to syntax. Use UUID of machine found in sentinels section
    AgentUUID = "lots of characters"
        # This will trigger every log for that endpoint
